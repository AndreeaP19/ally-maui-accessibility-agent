<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:strings="clr-namespace:SampleApp.Resources.Localization"
             x:Class="SampleApp.Views.LoginPage"
             Title="Login">
    <VerticalStackLayout Padding="24" Spacing="16">

        <ImageButton Source="back_arrow.png"
                     Clicked="OnBackTapped"
                     HorizontalOptions="Start"
                     SemanticProperties.Description="{strings:Localize A11y_BackButton_Description}" />

        <Label Text="Welcome back"
               FontSize="24"
               SemanticProperties.HeadingLevel="Level1" />

        <Entry x:Name="EmailEntry"
               Placeholder="Email" />

        <Entry x:Name="PasswordEntry"
               Placeholder="Password"
               IsPassword="True" />

        <HorizontalStackLayout Spacing="8">
            <CheckBox x:Name="RememberMeCheck"
                      SemanticProperties.Description="{strings:Localize A11y_RememberMeCheckbox_Description}" />
            <Label Text="Remember me" />
        </HorizontalStackLayout>

        <Button Text="Log In"
                Clicked="OnLoginClicked"
                SemanticProperties.Hint="{strings:Localize A11y_LoginBtn_Hint}" />

        <Button Text="Forgot Password?"
                Clicked="OnForgotPasswordTapped"
                SemanticProperties.Hint="{strings:Localize A11y_ForgotPasswordBtn_Hint}" />

    </VerticalStackLayout>
</ContentPage>
